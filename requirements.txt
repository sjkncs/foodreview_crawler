# ── 核心 Web 框架 ──────────────────────────────────────────────────
nicegui>=2.0.0

# ── 浏览器自动化（爬虫主引擎）────────────────────────────────────
playwright>=1.44.0

# ── HTTP 客户端 ───────────────────────────────────────────────────
httpx>=0.27.0

# ── AI SDK（双模式）──────────────────────────────────────────────
# OpenAI 兼容模式（iflow / deepseek / qwen 等）
openai>=1.30.0
# Anthropic 官方模式（claude 模型）
anthropic>=0.30.0

# ── 中文分词（关键词提取降级，可选）─────────────────────────────
jieba>=0.42.1

# ── Excel 导出（可选，不安装则降级 CSV）──────────────────────────
openpyxl>=3.1.0

# ── OCR 引擎（三选一，按需安装）──────────────────────────────────
# 方案1：PaddleOCR（中文最佳）
#   pip install paddlepaddle paddleocr opencv-python
# 方案2：Tesseract（轻量备用）
#   pip install pytesseract Pillow
#   系统需安装 tesseract-ocr 和 chi_sim 语言包

# ── 工具 ─────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── 舆情监控定时调度 ──────────────────────────────────────────────
schedule>=1.2.0
